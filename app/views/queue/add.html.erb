<div class="container">
  <div class="page-header">
    <h1>Select your top food picks</h1>
  </div>

  <%= simple_form_for :person do |f| %>
      <%= f.input :name, label: 'Hipchat @id' %>
      <%= f.input :first, collection: FoodCats.catlist, label: 'First choice.'%>
      <%= f.input :second, collection: FoodCats.catlist, label: 'Second choice.' %>
      <%= f.input :third, collection: FoodCats.catlist, label: 'Third choice.' %>
      <%= f.button :submit, value: 'Queue', class: 'btn btn-primary' %>
  <% end %>
</div>


<script>
    function validateId() {
        var x = document.forms["idForm"]["person_name"].value;
        if (x == null || x == "") {
            alert("ID can't be blank");
            return false;
        }

        if (x.charAt(0) != '@'){
            alert("IDs start with @");
            return false;
        }
    }
</script>